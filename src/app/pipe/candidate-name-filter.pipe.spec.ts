import { CandidateNameFilterPipe } from './candidate-name-filter.pipe';

describe('CandidateNameFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CandidateNameFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
